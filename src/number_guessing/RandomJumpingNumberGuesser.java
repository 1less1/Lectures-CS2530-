package number_guessing;

public class RandomJumpingNumberGuesser implements NumberGuessingPlayer {
    @Override
    public Integer getGuess() {
        return null;
    }

    // Move the next guess in the right direction (e.g. higher if I need to be higher)
    // but the amount it moves is randomly determined.
    @Override
    public void shareGuessResult(Integer result) {

    }
}
